publish_mode: editorial_workflow
media_folder: "content/images/uploads"
public_folder: "/images/uploads"
collections:
  - name: teachers
    label: /teachers
    folder: content/teachers
    extension: gs
    format: yaml-frontmatter
    create: true
    fields:
      - label: Title
        name: title
        widget: string
      - label: Body
        name: body
        widget: markdown
  - name: root
    label: /
    folder: content/
    extension: gs
    format: yaml-frontmatter
    create: true
    fields:
      - label: Title
        name: title
        widget: string
      - label: Body
        name: body
        widget: markdown
backend:
  name: github
  repo: DFE-Digital/content-prototype
  site_domain: remote-learning-prototype.london.cloudapps.digital

